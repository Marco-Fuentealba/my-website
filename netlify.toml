[build]
  publish = "."
  functions = "netlify/functions"

# Friendly API path so your frontend can keep using /api/contact
[[redirects]]
  from = "/api/contact"
  to = "/.netlify/functions/contact"
  status = 200
